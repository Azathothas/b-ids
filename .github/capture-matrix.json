{
  "comment": [
    "The capture matrix, in ONE place. .github/workflows/capture.yml reads this",
    "to build its job matrix and scripts/common/check-coverage reads it to say",
    "which cells have a profile, so the plan and the report cannot disagree.",
    "A matrix written into the workflow and a coverage report written from",
    "somewhere else is a value in two places with no check that they agree.",
    "",
    "enabled false is a cell that is PLANNED and not attempted. It is reported",
    "as such rather than dropped, because a coverage report that lists only what",
    "was tried cannot show what is missing. TODO/corpus.md, CORPUS-02.",
    "",
    "required true means check-coverage --require-rows can be asked to fail on",
    "it. Nothing is required by default: the flag names what a caller cares",
    "about.",
    "",
    "os is the runner label and it is the only field the workflow needs that the",
    "corpus does not have. browser, channel and platform are the three keys a",
    "profile's route is derived from, spelled exactly as the corpus spells them.",
    "",
    "route says HOW the lane gets the browser, and it is the field DRIVER-08",
    "added. installed takes whatever the runner image has, which is what every",
    "capture so far did and why every published profile carries",
    "captured.acquisition null. vendor purges the machine and installs the",
    "current branded build from the vendor's own channel. for-testing purges and",
    "installs an exact build from the automation index, which is UNBRANDED.",
    "scripts/common/provision-browser.sh is the tool and --route is the flag.",
    "",
    "build names the exact build a for-testing cell wants. The automation index",
    "publishes a SUBSET of builds: measured 2026-09-02 it carried 67 builds of",
    "Chrome 151 and neither of the two the runner images served, so a build here",
    "is one the index was checked to have."
  ],
  "schema_version": 2,
  "cells": [
    {
      "browser": "chrome",
      "channel": "stable",
      "platform": "linux64",
      "os": "ubuntu-latest",
      "route": "installed",
      "branded": true,
      "build": null,
      "enabled": true,
      "required": true,
      "why": "The cheapest lane there is: the runner image ships Chrome, so this needs no acquisition at all."
    },
    {
      "browser": "chrome",
      "channel": "stable",
      "platform": "win64",
      "os": "windows-latest",
      "route": "installed",
      "branded": true,
      "build": null,
      "enabled": true,
      "required": true,
      "why": "The one platform the corpus already has, captured again on a runner rather than on a laptop. Two profiles of one build on two platforms is what decides whether the TLS half is platform-independent."
    },
    {
      "browser": "edge",
      "channel": "stable",
      "platform": "linux64",
      "os": "ubuntu-latest",
      "route": "installed",
      "branded": true,
      "build": null,
      "enabled": true,
      "required": true,
      "why": "The same engine with a different brand list and User-Agent, so it isolates branding from engine at almost no cost. Enabled 2026-09-02: the driver resolved Edge 151.0.4129.101 at /usr/bin/microsoft-edge on the ubuntu-latest image in capture.yml run 33579619515, and b-ids-driver drive takes a --browser switch now, so the lane drives the family this row names."
    },
    {
      "browser": "chrome",
      "channel": "for-testing",
      "platform": "linux64",
      "os": "ubuntu-latest",
      "route": "for-testing",
      "branded": false,
      "build": "151.0.7922.76",
      "enabled": false,
      "required": false,
      "why": "The unbranded build of the version the corpus already holds from a laptop, so one build across two platforms and two brands becomes comparable. Planned and not attempted: the channel token this row uses is not in the schema's Channel vocabulary, and the corpus route is keyed by browser, channel, platform and version with no room for branded, so a branded and an unbranded build of one version would publish at one path. TODO/PROGRESS.md carries the question and DRIVER-06 owns the difference."
    },
    {
      "browser": "chrome",
      "channel": "for-testing",
      "platform": "win64",
      "os": "windows-latest",
      "route": "for-testing",
      "branded": false,
      "build": "151.0.7922.76",
      "enabled": false,
      "required": false,
      "why": "The other half of the pair above, and the one that measures whether the TLS half is platform-independent at a build both platforms genuinely run. Blocked on the same vocabulary question."
    },
    {
      "browser": "chrome",
      "channel": "stable",
      "platform": "macos-arm64",
      "os": "macos-latest",
      "route": "installed",
      "branded": true,
      "build": null,
      "enabled": false,
      "required": false,
      "why": "A third platform and a second architecture. Waiting on DRIVER-05 having a route that serves a macOS build, and on the identity writer taking its platform from the run rather than from a two-way branch. The automation index spells this platform mac-arm64 and the corpus spells it macos-arm64, so a for-testing cell here translates deliberately."
    },
    {
      "browser": "chrome",
      "channel": "beta",
      "platform": "linux64",
      "os": "ubuntu-latest",
      "route": "vendor",
      "branded": true,
      "build": null,
      "enabled": false,
      "required": false,
      "why": "How the project gets ahead of a release rather than behind it. Waiting on acquisition: no runner image ships a beta channel, and provision-browser's vendor route serves the stable channel only."
    },
    {
      "browser": "firefox",
      "channel": "stable",
      "platform": "linux64",
      "os": "ubuntu-latest",
      "route": "installed",
      "branded": true,
      "build": null,
      "enabled": false,
      "required": false,
      "why": "A genuinely different TLS stack and the highest-value non-Chrome lane. Waiting on the resolver knowing the family at all."
    }
  ]
}
