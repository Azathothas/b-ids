[package]
name = "b-ids-emit"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# ⛔ Nothing here is released. TODO/publish.md, PUB-05, is the entry that
# decides what a package registry ever sees.
publish = false

[lints]
workspace = true

[dependencies]
b-ids-schema = { path = "../b-ids-schema" }
# ⚠ THE MATRIX IS SERIALISED, never formatted. EMIT-01.
serde.workspace = true

[dev-dependencies]
# ⚠ THE ESCAPE HATCH SUITE READS THE PUBLISHED CORPUS, because a fixture would
# prove the emitter can write a hello somebody made up. TODO/emitters.md,
# EMIT-02.
serde_json.workspace = true
